version: '2'

services:
  3proxy:
    image: manhnv56/3proxy3131
    container_name: 3proxy
    restart: always
    environment:
      - HTTP_PORT=3132
      - SOCKS_PORT=1083
    volumes:
       - ./passwd:/etc/3proxy/passwd:ro
    ports:
      - "1083:1083"
      - "3132:3132"
